
const FunFun = () => {
    
        var images = [],

        index = 0;
        images[0] = 
        images[1] = 
        images[2] = 
        index = Math.floor(Math.random() * images.length);
        document.write(images[index]);

    return (
        <>
        
        
        </>
    );

};

export default FunFun;